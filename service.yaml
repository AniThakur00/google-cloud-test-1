apiVersion: v1
kind: Service
metadata:
  name: project-one-service
spec:
  selector:
    app: project-one  
  ports:
    - protocol: TCP
      port: 80        # Port on the service
      targetPort: 8000  # Port your application is listening on in the pod
  type: LoadBalancer  # Expose via LoadBalancer
